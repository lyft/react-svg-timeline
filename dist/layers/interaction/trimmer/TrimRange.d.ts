/// <reference types="react" />
interface Props {
    startX: number;
    endX: number;
    height: number;
    width: number;
}
export declare function TrimRange({ startX, endX, height, width }: Props): JSX.Element;
export {};
